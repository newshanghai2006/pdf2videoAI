flask>=3.0
# 注意：不要重新加入 pypdfium2。它在本项目 venv 中首次 import 需约 30 分钟
# （原生 pdfium.dll 被安全软件极慢扫描），会导致上传/渲染卡死。PDF 渲染已改为
# 通过 pdf_helper.py 子进程调用系统 Python 的 PyMuPDF(fitz) 完成，无需 pdfium。
PyMuPDF>=1.24
Pillow>=10.0
opencv-python>=4.8
numpy>=1.24
rapidocr-onnxruntime>=1.2
# Optional alternative OCR engine (install separately when selected in UI):
# easyocr>=1.7
openai>=1.0
edge-tts>=6.1
